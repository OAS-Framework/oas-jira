# Playbooks

* [Probing a package against a real released OAS kernel in isolation](isolated-released-kernel-probe.md) - How to drive a package through a released kernel without trusting the ambient CLI, the host's tools, or the developer's own OAS deployment.
