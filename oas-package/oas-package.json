{
  "package": "oas.jira",
  "version": "2.0.0",
  "description": "Official OAS tasks integration for Jira using Atlassian's acli command-line interface.",
  "compatibility": {
    "oas": ">=0.20.0"
  },
  "capabilities": [
    "capabilities/oas-jira"
  ],
  "configTemplates": {
    "default": {
      "path": "config-templates/default/oas-config.yaml",
      "description": "Tasks layer bound to oas.jira for every agent, with the Jira site and project left for the adopter to set.",
      "default": true
    }
  }
}
